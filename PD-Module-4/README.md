# Module 4: Timing Analysis, Clock Tree Synthesis & Physical Design

## 📌 Project Overview:

This project focuses on the physical-design and timing-analysis stages of an RTL-to-GDSII flow using the PicoRV32A design, OpenLane, OpenROAD, and the SKY130A technology.

The module covers:

RTL Design → Synthesis → Cell Mapping → Floorplanning → Placement → Clock Tree Synthesis → Timing Analysis → Routing → Physical Verification

The project also studies important timing concepts such as:

* **Setup time**
* **Hold time**
* **Data arrival time**
* **Data required time**
* **Slack**
* **Clock skew**
* **Clock latency**
* **Clock-tree buffering**
* **Crosstalk-induced delay**
* **Real-clock timing**
* **Ideal-clock timing**

## 🎯 Objectives:

The main objectives of this project are:

1.To understand the RTL-to-GDSII physical-design flow.

2.To configure and execute an OpenLane design flow.

3.To analyse the PicoRV32A design using the SKY130A PDK.

4.To understand synthesis and technology mapping.

5.To analyse the number and types of standard cells generated after synthesis.

6.To understand placement and physical cell distribution.

7.To study Clock Tree Synthesis (CTS).

8.To understand clock buffering and clock distribution.

9.To analyse clock skew and clock latency.

10.To understand setup and hold timing analysis.

11.To study the effect of real interconnect RC delay.

12.To understand crosstalk-induced delay and skew.

13.To analyse timing reports generated by OpenROAD.

14.To distinguish between ideal-clock and propagated-clock timing analysis.

15.To understand timing closure and slack improvement.

## 🔧 Tools & Technologies

| **Tool / Technology** | **Purpose**                                               |
| --------------------- | --------------------------------------------------------- |
| **OpenLane**          | RTL-to-GDSII physical-design flow                         |
| **OpenROAD**          | Physical implementation and timing analysis               |
| **Yosys**             | RTL synthesis and logic optimization                      |
| **SKY130A PDK**       | CMOS technology and standard-cell library                 |
| **Magic VLSI**        | Layout viewing and physical verification                  |
| **PicoRV32A**         | RISC-V processor design used as the implementation target |
| **Linux Terminal**    | Flow execution and analysis                               |
| **Tcl**               | OpenLane/OpenROAD configuration and scripting             |
| **Git & GitHub**      | Version control and documentation                         |

## 🔬 Project Workflow:
```
RTL Design

↓

OpenLane Configuration

↓

Logic Synthesis

↓

Technology Mapping

↓

Design Statistics

↓

Floorplanning

↓

Placement

↓

Clock Tree Synthesis

↓

Routing

↓

Parasitic Extraction

↓

Static Timing Analysis

↓

Setup/Hold Verification

↓

Physical Verification
```

## 1. Physical Design Coordinate / Layer Information:

It shows coordinate information associated with physical-design layers such as li1, met1, met2, met3, met4, and met5.

The displayed coordinates represent the physical boundaries or locations associated with different routing layers.

The information demonstrates that physical design is represented using geometric coordinates rather than only logical connections.

<img width="940" height="544" alt="image" src="https://github.com/user-attachments/assets/460b53d1-16d5-4a7f-824b-5758aad39e4e" />

Important concepts shown include:

* Local interconnect

* Metal layers

* X/Y coordinates

* Physical routing dimensions

* Layer-specific geometry

This information is useful when analysing the physical location and routing of cells and nets.

## 2. Magic VLSI Layout and DRC Environment:

It shows the Magic VLSI layout environment using the SKY130A technology.

The layout contains multiple physical layers representing transistor regions, contacts, polysilicon, diffusion, and metal.

A Magic console window is also visible. The grid command indicates a physical layout grid with dimensions related to the SKY130A technology.

<img width="940" height="474" alt="image" src="https://github.com/user-attachments/assets/e5af02df-e4c8-4eeb-88ed-fdaaa12585bf" />

The image demonstrates:

* Physical layout editing

* SKY130A technology

* Layer-based representation

* Layout grid

* Design-rule-checking environment

* Interactive physical-design debugging

## 3. Standard-Cell Physical Layout:

This image presents a zoomed physical layout of a standard cell.

Different colours and patterns represent different layers of the CMOS implementation.

<img width="690" height="425" alt="image" src="https://github.com/user-attachments/assets/7e80e3b9-9f55-4630-ba6a-898a4e29c2df" />

The layout contains:

* Metal/interconnect regions

* Diffusion regions

* Polysilicon

* Contacts

* Transistor structures

* Power/ground connections

The image demonstrates how a logical standard cell is converted into a physical geometric structure that can be placed and routed in an integrated circuit.

## 4. OpenLane Design Configuration:

This image shows the OpenLane configuration for the PicoRV32A design.

Important parameters visible in the configuration include:

DESIGN_NAME = picorv32a
CLOCK_PERIOD = 5.000
CLOCK_PORT = clk

<img width="940" height="543" alt="image" src="https://github.com/user-attachments/assets/32023345-9ca1-4fdf-9006-05a9ea94c2eb" />

The configuration also specifies the RTL source file and the SDC timing constraint file.

The design configuration establishes:

* Design name

* RTL source

* SDC constraints

* Clock period

* Clock port

* OpenLane environment

* Standard-cell configuration

The clock period of 5 ns corresponds to a nominal 200 MHz clock frequency.

## 5. OpenLane Initialization:

This image shows the OpenLane environment being launched from the Linux terminal.

The OpenLane version shown is:

Version: rc2

<img width="940" height="208" alt="image" src="https://github.com/user-attachments/assets/243b5fe3-254d-4398-9045-055a600964c4" />

The image demonstrates:

* OpenLane startup

* Docker-based execution environment

* OpenLane version information

* Linux terminal workflow

OpenLane provides an automated flow connecting synthesis, floorplanning, placement, CTS, routing, and physical verification.

## 6. Logic Synthesis and Cell Mapping:

This image shows synthesis output generated for the PicoRV32A design.

The output includes mapping of generic flip-flop structures into SKY130 standard cells.

For example:

mapped 1634 $DFF_P cells

<img width="940" height="613" alt="image" src="https://github.com/user-attachments/assets/3b197491-fac0-4715-8cb5-65d3c180e247" />

The design statistics show:

* Number of wires

* Number of wire bits

* Number of public wires

* Number of cells

* Logic-gate distribution

* Technology-mapped flip-flops

The synthesis process converts RTL logic into a gate-level representation using cells available in the target technology.

## 7. Post-Synthesis Design Statistics:

This image shows additional synthesis statistics after technology mapping.

The output lists different SKY130 standard cells such as:

* AND gates

* OR gates

* NAND gates

* NOR gates

* Inverters

* Buffers

* Flip-flops

The image also reports the calculated chip area:

Chip area for module 'picorv32a'

<img width="940" height="749" alt="image" src="https://github.com/user-attachments/assets/46817632-ed8f-4afa-bb70-91b5339b48a8" />

The statistics help evaluate the complexity and physical size of the synthesized design.

Important parameters include:

* Cell count

* Standard-cell composition

* Wire count

* Chip area

*  Technology-mapped cells

## 8. Static Timing Analysis — Slack Violation:

This image shows a timing-analysis report from the physical-design flow.

The report contains:

* Data arrival time

* Data required time

* Library setup time

* Clock information

* Clock network delay

* Slack

The displayed result includes:

slack (VIOLATED)

<img width="940" height="1005" alt="image" src="https://github.com/user-attachments/assets/37259769-d279-4c0c-a5a7-fc246cd2e457" />

A negative slack indicates that the analysed timing path does not satisfy the specified timing requirement.

The image demonstrates why Static Timing Analysis (STA) is necessary during physical implementation.

Timing violations must be addressed through optimization such as:

* Cell sizing

* Buffer insertion

* Placement optimization

* Clock optimization

* Routing optimization

## 9. Power-Aware Clock Tree Synthesis:

This image explains the concept of Power-Aware Clock Tree Synthesis (CTS).

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/d4482e5e-4055-4ea9-a9bd-04acde0d2267" />

The diagram contains two levels of clock buffering:

* Level 1

* Level 2

It also shows delay tables based on:

* Input slew

* Output load

The delay tables represent different buffer delay values for different combinations of input transition and capacitive load.

The example demonstrates that clock-tree design must consider both:

* Timing

* Power

The clock network is constructed so that multiple sequential elements receive the clock with controlled delay and balanced loading.

## 10. Complete Physical Layout View:

This image shows a large-scale physical layout generated during the physical-design flow.

The dense structure represents a large number of placed standard cells and routing structures.

<img width="940" height="529" alt="image" src="https://github.com/user-attachments/assets/22ac93c7-d9b0-4021-9123-4d2e79289693" />

The layout demonstrates the transition from:

## Logical Design → Physical Cell Placement

The dense cell arrangement is typical of a processor-scale design containing thousands of standard cells.

This stage is important for analysing:

* Cell density

* Placement

* Routing resources

* Physical utilization

* Overall chip organization

## 11. Detailed Standard-Cell Placement and Connectivity:

This image shows a detailed physical-design view containing placed standard cells and connectivity information.

<img width="940" height="601" alt="image" src="https://github.com/user-attachments/assets/dc90b866-5792-4171-a058-13b847cbfdc9" />

Several SKY130 standard cells are visible, including:

* D flip-flops

* NAND gates

* NOR gates

* AND gates

* Other combinational cells

The design also contains clock-related and power-related structures.

The image demonstrates how individual standard cells are physically arranged and interconnected after placement.

## 12. Clock Tree Synthesis:

This diagram illustrates Clock Tree Synthesis.

Two flip-flops, **FF1** and **FF2**, receive the same clock source through a clock distribution network.

The diagram defines skew as:

Skew = t2 − t1

The desired condition is approximately:

Skew ≈ 0 ps

<img width="940" height="853" alt="image" src="https://github.com/user-attachments/assets/2cac79fc-cf24-40cd-b248-4f27b50fe6ea" />

The objective of CTS is therefore to distribute the clock so that clock arrival times at sequential elements are as closely matched as practical.

Clock skew directly affects:

* Setup timing

* Hold timing

* Maximum operating frequency

* Timing closure

## 13. Clock Tree Buffering:

This image demonstrates clock-tree buffering.

The clock network is divided into multiple branches, with buffers inserted to drive different loads.

The diagram also illustrates the RC behaviour of a buffer and the concept of propagation delay.

Clock buffering is required because a single clock source cannot efficiently drive a large number of sequential elements directly.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/0c87a996-26f9-49af-aabf-30b0a5061703" />

Buffers help:

* Increase drive strength

* Control transition time

* Reduce excessive delay

* Distribute clock signals

* Balance clock arrival times

## 14. Crosstalk Delta Delay and Clock Skew:

This image explains the effect of **crosstalk-induced delta delay on clock skew.**

Coupling capacitance between neighbouring wires can change the delay of a signal.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/80edcfd8-56d2-4c2d-a11a-c7a09699a831" />

The figure compares:

Before Crosstalk
Delay = D

with:

After Crosstalk
Delay = D + Δ

The diagram further represents the relationship:

L1 = L2 + Δ

and:

SKEW = L1 − (L2 + Δ)

Crosstalk therefore has the potential to change signal delay and consequently affect clock skew.

This is particularly important in advanced physical-design timing analysis where interconnect effects become significant.

## 15. Placement Analysis and Legality Checks:

This image shows the result of the placement stage.

The report provides design statistics such as:

* Total cells

* Fixed cells

* Total nets

* Design area

* Utilization

* Number of rows

The image also shows placement evaluation metrics such as:

* Average displacement

* Maximum displacement

* HPWL

* Average displacement per site

* Average displacement per row

At the end of the report, several legality checks are shown as:

row_check  ==> PASS
site_check ==> PASS
power_check ==> PASS
edge_check ==> PASS
placed_check ==> PASS
overlap_check ==> PASS

These checks verify that the placed design satisfies important physical constraints.

## 16. Hold Analysis with Real Clocks:

This image illustrates hold-time analysis using real clock propagation.

The timing path consists of:

**Launch Flip-Flop → Combinational Logic → Capture Flip-Flop**

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/4bcff447-4c39-4391-8f53-b669070a3f8f" />

The clock path contains multiple clock buffers.

The figure defines:

* Data arrival time

* Data required time

* Clock uncertainty

* Hold time

* Clock propagation delay

* Slack

The hold condition is represented as:

θ + Δ₁ > H + Δ₂ + HU

where the clock and data delays must satisfy the required hold relationship.

The example uses:

Clock Frequency = 1 GHz
Clock Period = 1 ns

This demonstrates how real clock propagation influences timing analysis.

## 17. Hold Analysis Using Flip-Flop Internal Structure:

This image provides a more detailed explanation of hold timing using the internal structure of a flip-flop.

The flip-flop is represented using two multiplexers:

* Mux1

* Mux2

The diagram shows how data propagates through the internal master/slave structure.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/31741c35-0945-405f-98c7-be58449410de" />

The waveform illustrates the relationship between:

* Clock

* D

* Internal node \(Q_M\)

* Output \(Q\)

The diagram helps explain why a finite amount of time is required for data to propagate through the internal flip-flop structure.

This internal delay contributes to the overall hold-time requirement.

## 18. Real-Clock Timing Paths:

This image shows timing analysis using real clocks and real interconnect delays.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/2cd14f4e-0b57-4b5c-b01e-89c00453c381" />

The design contains several timing paths between:

* Input ports

* Flip-flops

* Buffers

* Combinational logic

* Output ports

* Clock network

The image identifies timing-path delays using:

Real wire RC delay
+
Buffer delay

The total delay therefore includes both cell delay and interconnect delay.

The hold condition is represented as:

θ + Δ₁ > H + Δ₂ + HU

This analysis is more realistic than an ideal-clock analysis because actual physical delays are included.

## 19. Clock Propagation and Timing Report:

This image shows an OpenROAD timing report containing detailed clock propagation information.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/e22eee41-c9be-4cfc-96f3-29fff19fd38f" />

The report includes:

* Clock source

* Clock network delay

* Clock buffer delays

* Clock reconvergence pessimism

* Library setup time

* Data required time

* Data arrival time

* Slack

The timing result shown includes a negative slack value:

slack (VIOLATED)

This indicates that the analysed timing path does not satisfy the timing requirement under the reported conditions.

The report is useful for identifying the specific contributors to the timing violation.

## 20. Clock Skew Report and CTS Buffer Configuration:

This image shows clock timing results after clock propagation.

The report contains:

slack (MET)

indicating that the analysed timing condition is satisfied for that path.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/16704675-c56f-44cc-965f-731aa87b316c" />

The image also shows:

report_clock_skew -hold
report_clock_skew -setup

The reported clock skew is:

0.28

The lower portion of the image shows the CTS clock-buffer list being configured.

This demonstrates how CTS configuration can be modified by selecting different clock-buffer cells.

## 21. Placement Results After Configuration:

This image shows another placement-analysis result for the PicoRV32A design.

<img width="940" height="434" alt="image" src="https://github.com/user-attachments/assets/d0ceb389-a03e-460f-b00a-3a760e97453d" />

The report includes:

* Total cells

* Fixed cells

* Total nets

* Design area

* Utilization

* Number of rows

* HPWL

*Placement displacement

The legality checks again show successful results:

row_check     ==> PASS
site_check    ==> PASS
power_check   ==> PASS
edge_check    ==> PASS
placed_check  ==> PASS
overlap_check ==> PASS

The image demonstrates a physically valid placement before subsequent physical-design stages.

## 22. Timing Analysis with Ideal Clocks:

This image illustrates timing analysis using **ideal clocks.**

<img width="940" height="603" alt="image" src="https://github.com/user-attachments/assets/70752c94-829b-49e9-8b84-1886856cc095" />

The design contains multiple timing paths involving:

* Flip-flops

* Buffers

* Combinational logic

* Clock networks

* Decoupling structures

The timing paths are highlighted to identify the paths between different sequential elements.

Ideal-clock analysis assumes an idealized clock distribution before actual clock-tree delays are considered.

This analysis is useful for separating:

**Data-path timing effects**

from:

**Clock-network timing effects**

The diagram also highlights buffers and clock-related structures used during the physical-design process.

## 23. Setup Analysis with Ideal Clocks:

This image explains setup-time analysis using an ideal clock.

The flip-flop is represented using its internal multiplexer structure:

* Mux1

* Mux2

The timing waveform shows the relationship between:

* Clock

* Data \(D\)

* Internal node \(Q_M\)

* Output \(Q\)

The diagram explains that data must reach the internal capture point sufficiently before the active clock edge.

The key concept is:

A finite time is required before the clock edge for the data to reach the internal node.

This internal delay contributes to the setup time of the flip-flop.

**📊 Important Timing Concepts:**

**Setup Time**

Setup time is the minimum amount of time for which input data must be stable before the active clock edge.

Data must arrive sufficiently before:
                    ↓
                Clock Edge

A setup violation occurs when the data arrives too late.

**Hold Time**

Hold time is the minimum amount of time for which data must remain stable after the active clock edge.

Clock Edge
    ↓
Data must remain stable

A hold violation occurs when data changes too soon after the clock edge.

<img width="940" height="367" alt="image" src="https://github.com/user-attachments/assets/f9560748-dd69-4266-929a-2eed92d1fbc0" />

**Data Arrival Time**

Data arrival time represents the time at which data reaches the capture element.

It depends on:

* Cell delay

* Interconnect delay

* Buffer delay

* Routing parasitics

**Data Required Time**

Data required time represents the latest acceptable arrival time for the data to satisfy the timing constraint.

**Slack**

Slack represents the difference between the required and actual timing.

For setup analysis:

Slack = Data Required Time − Data Arrival Time

Generally:

Positive Slack → Timing requirement satisfied
Zero Slack     → Critical timing boundary
Negative Slack → Timing violation

# ⏱️ Real Clock vs Ideal Clock

| **Parameter**      | **Ideal Clock**            | **Real / Propagated Clock** |
| ------------------ | -------------------------- | --------------------------- |
| Clock network      | Idealized                  | Physically implemented      |
| Clock buffer delay | Not fully represented      | Included                    |
| Clock skew         | Idealized                  | Included                    |
| Wire RC delay      | Simplified                 | Included                    |
| CTS effects        | Not represented completely | Represented                 |
| Timing accuracy    | Preliminary                | More realistic              |

### 🌳 Clock Tree Synthesis Flow

The CTS process can be summarized as:

**Clock Source
↓
Clock Buffering
↓
Clock Branching
↓
Clock Distribution
↓
Sequential Elements**

The major objectives are:

Reduce clock skew
Control clock latency
Maintain acceptable slew
Handle capacitive load
Distribute the clock efficiently
Satisfy setup and hold requirements

## 🧮 Timing Closure

Timing closure is the process of modifying the physical implementation until the design satisfies its timing constraints.

Typical optimization techniques include:

1. Cell sizing

2. Buffer insertion

3. Buffer resizing

4. Placement optimization

5. Clock-tree optimization

6. Routing optimization

7. Reduction of excessive wire delay

8. Reduction of clock skew

9. Optimization of critical paths

The final objective is to obtain timing results with acceptable slack for all required paths.

## 🏁 Conclusion

Module 4 demonstrates the transition from a synthesized RTL design to a physically implemented and timing-verified design.

The complete flow connects:

## Synthesis → Placement → Clock Tree Synthesis → Routing → Parasitic Effects → Static Timing Analysis → Timing Closure

The images demonstrate the practical implementation of the PicoRV32A design using the SKY130A technology and OpenLane/OpenROAD flow.

The module particularly emphasizes that physical implementation affects timing through:

* Cell delays

* Wire RC delays

* Clock latency

* Clock skew

* Crosstalk

* Buffer delays

* Setup and hold requirements

Thus, successful physical design requires both **geometrical correctness and timing correctness.**




